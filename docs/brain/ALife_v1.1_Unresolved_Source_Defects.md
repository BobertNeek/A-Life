# A-Life v1.1 unresolved source defects

Status at repair-only stop: **no known D01-D27 source deficiency remains open**.

This statement means the repair ledger maps every known plan deficiency to a production source change or an explicit architecture ruling. It does not mean the animal is behaviorally intelligent, fast, balanced, or release-ready.

## Non-blocking source concerns for independent review

1. The root `AGENTS.md` still points to absent `docs/master_spec.md` and `docs/architecture_decisions.md`. The controlling v1.1 spec and current architecture/reference documents were used instead. The missing pointers should be judged as repository-instruction hygiene, not silently reconstructed.
2. The final compile may retain warnings in older curated-founder and game-app paths. Warnings are not treated as proof of a source defect, but the reviewer should flag any that expose a real disconnected production path.
3. Old exact cognitive checkpoint schemas cannot provide exact continuity for state they never stored. The repaired loader rejects them rather than inventing state. This is a deliberate compatibility cost, not an unresolved implementation.

## Deferred validation debt (D28, outside this run)

- behavioral ability, few-shot avoidance, reversal learning, and EI1 corpus;
- ablations for prediction, topology, dendrites, structural growth, sleep, and cognitive cost;
- 30/50/100/250/500 resident scaling, frame time, readback, memory, and checkpoint cost;
- long ecology, persistence, sleep, and multi-generation evolution runs;
- usability, external playtesting, alpha readiness, CI, and release claims.

No inference from the earlier zero-valued screens is promoted to a current-source verdict. Source changed substantially after those measurements.

