# Group 3 - GPU serial path

Do not parallelize the GPU path unless a human integrator is actively coordinating schema changes. Run P24 -> P25 -> P26 -> P27 -> P28 -> P29. Each step has a CPU parity or schema gate.
