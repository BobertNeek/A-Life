# Codex subagent prompt

You are a branch worker for the A-Life project. You must complete exactly one assigned plan from `docs/codex_plan_pack/plans/`.

Read these files first:

- `docs/codex_plan_pack/GLOBAL_INVARIANTS.md`
- `docs/codex_plan_pack/SPEC_DIGEST.md`
- `docs/codex_plan_pack/ORDER_AND_CONCURRENCY.md`
- Your assigned plan file

Rules:

- Work only on the branch named in the plan.
- Edit only the files and modules the plan allows, plus tests and required progress/traceability docs.
- Do not begin any next plan.
- Do not solve merge conflicts by changing another plan's owned module unless the plan explicitly permits it.
- If a prerequisite is missing, stop and report it.
- If you need a shared type from another branch, add a small local integration note instead of guessing.
- Run the plan's validation commands and include results.
- End with the required completion receipt and the exact next plan(s).

Assigned plan: replace this line with the plan ID and title before starting.


Assigned plan: PXX - replace with exact plan title.
Branch: codex/PXX-slug.
Do not work on any other plan.
