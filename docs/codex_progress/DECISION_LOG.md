# Decision log

Record architecture decisions here. Do not record every tiny implementation detail. Record decisions that affect public contracts, dependency boundaries, schemas, runtime behavior, or future plans.

| Date | Plan | Decision | Rationale | Alternatives rejected | Follow-up |
|---|---|---|---|---|---|
| 2026-06-10 | seed | Keep `alife_core` engine-independent | Core cognition must be testable without Bevy/GPU | Direct Bevy Entity/Vec3/Quat in core | Enforce in P01/P02 |
| 2026-06-10 | seed | Split runtime `ExperiencePatch` from packed logging | Runtime cognition and binary export have conflicting ownership/layout needs | One struct for everything | Implement in P10/P11 |
| 2026-06-10 | P00 | Normalize the plan pack to `docs/codex_plan_pack/` | The master prompt hardcodes this path and future branches need one stable location | Leaving the pack only at `docs/alife_codex_plan_pack_v1/` | Audit and commit stable pack path in P00 |
| 2026-06-10 | P01 | Remove tracked `a_life_revised_spec_pack/` mirror | It duplicated the active workspace scaffold and docs, increasing merge and validation noise | Archiving duplicate code under docs/specs | Keep source-of-truth specs in `docs/` |
| 2026-06-10 | P01 | Use `scripts/graphify.sh hook-check` for Codex Graphify hook | Hooks must be portable and optional; the script already skips cleanly when Graphify is unavailable | Absolute Windows user-profile Graphify executable path | P01 validation records WSL bash limitation and Git Bash substitute |
| 2026-06-10 | P02 | Put schema/versioning conventions under `docs/architecture/` | Version rules apply across ABI, logs, GPU layouts, saves, and offline tools, so they need a stable architecture home | Burying version rules only in plan receipts | P04/P11/P24/P30/P34 must follow this convention |
| 2026-06-10 | P02 | Use `alife_tools` integration tests for repo-level invariant harness | The tooling crate can validate docs/progress/file hygiene without polluting `alife_core` domain contracts | Root integration tests without a root package | Future plans can add repo invariant tests here |
| 2026-06-10 | P03 | Use native `alife_core` math/unit wrappers instead of `glam` | Avoids introducing another math dependency and keeps Bevy adapter conversion explicit | Adding `glam` to core | P21 can implement adapter-side conversions |
| 2026-06-10 | P03 | Document Bevy entity/math mapping in `docs/architecture/core_adapter_boundary.md` | Core source boundary checks intentionally flag engine symbols in core source and tests | Keeping engine type names in core comments | Keep adapter mapping docs outside core source |
| 2026-06-10 | P04 | Centralize public schema versions in `SchemaVersions::CURRENT` | Parallel P05-P09 branches need one shared source for ABI/schema version constants | Per-module independent `ABI_VERSION = 1` constants | Future schema changes must update central versions and tests |
| 2026-06-10 | P04 | Use compact enum diagnostics instead of string diagnostics | Logs/tests need stable typed assertions without allocating or freezing message text | String-only validation errors | Expand diagnostic codes as future plans add domains |
| 2026-06-11 | P04.5 | Freeze active gameplay transfer as compact action summaries only | P05-P09 need a shared CPU/GPU boundary before defining lobe, genome, endocrine, sensory, and action contracts | Bulk neural readback or arbitrary activation/weight readback in the standard tick | P24-P29 must preserve this in GPU contracts |
| 2026-06-11 | P04.5 | Use explicit formula-derived VRAM ledger totals for required classes and populations | Memory estimates must be auditable and not hand-wavy before concurrent contract work starts | Unvalidated prose-only estimates or compressed totals without rules | P05/P06/P14/P20/P24 must update formulas if budgets change |
| 2026-06-11 | P04.5 | Keep `AlphaMask` default hierarchical rather than dense per-synapse | Dense alpha masks would waste memory before benchmarks prove they are needed | Default dense per-synapse alpha storage | P06 owns detailed alpha semantics under this policy |
| 2026-06-11 | P04.5 | Do not add a new global invariant for P04.5 | Existing invariants already cover no active readback, bounded sparse buffers, CPU/GPU parity, and structured actions | Duplicating existing invariants in `GLOBAL_INVARIANTS.md` | Revisit only if later plans expose an uncovered rule |
| 2026-06-11 | P05 | Represent `Standard2048` as an explicit aligned lobe table with a 224-node logical motor ring | The master spec names canonical lobes and preserves the old 224-node motor ring only as the Standard2048 example, but does not give every numeric boundary | Keeping proportional equal splits or treating 2048 as the only viable layout | P10/P14 can consume the data-driven lobe layout instead of duplicating constants |
| 2026-06-11 | P05 | Store routing masks as versioned core metadata, not executable neural kernels | P05 must define projection eligibility and cadence without implementing runtime compute or GPU dispatch | Adding CPU/GPU neural projection code in P05 | P14 should translate this metadata into sparse projection schema fixtures |
| 2026-06-11 | P06 | Keep neural weight layers descriptor-only in `alife_core` | P06 is a contract scaffold; concrete sparse buffers belong to P14/P24 and must not allocate huge matrices in core | Dense vectors or storage-backed weight layers in `alife_core` | P14/P24 implement sparse CPU/GPU storage against these descriptors |
| 2026-06-11 | P06 | Gate inherited lifetime weights behind explicit Lamarckian flags defaulting off | The genotype/phenotype boundary must make inherited instinct immutable by default while preserving future experimental modes | Default `mutable_lifetime_weights_allowed` behavior or implicit lifetime carryover | P16/P33 can add explicit runtime/evolution experiments with tests |
| 2026-06-11 | P07 | Keep homeostatic snapshots and deltas as distinct bounded contracts | Snapshots represent state at a tick; deltas represent signed outcome/change values and must not be confused before experience sealing or learning | Anonymous raw arrays or one combined state/change struct | P10/P15 should use the snapshot/delta split when wiring ExperiencePatch and the reference brain loop |
| 2026-06-11 | P07 | Use deterministic linear drift/decay and threshold recovery as reference chemistry defaults | Exact biological equations are underspecified, so P07 needs simple testable defaults that reject invalid learning state and can be replaced later | Unbounded raw floats or hidden runtime-specific time resources | Future plans may replace formulas behind the same bounded/versioned contracts |
| 2026-06-11 | P08 | Keep sensory ABI numeric channels fixed while carrying semantic/Gaussian/SLM/school data as optional refs | ExperiencePatch, adapters, GPU input packing, and school systems need a stable engine-neutral sensory target without renderer or LLM coupling | Embedding Bevy entities, renderer objects, or hidden semantic vectors in core sensory snapshots | P10/P14/P21/P22 consume these refs through explicit adapter or packing contracts |
| 2026-06-11 | P09 | Bump `ActionAbi` to v2 for structured public commands | P09 expands the P04 placeholder command into a public ABI carrying action ID/kind, optional target entity/position, intensity, duration, confidence, source mask, teacher metadata, motor payload refs, and arbitration trace refs | Reusing v1 after a breaking command shape change | P10/P11 should consume v2 action commands in experience patches and logs |
| 2026-06-11 | P09 | Use deterministic CPU WTA arbitration as the reference baseline | Arbitration must be testable before GPU staging exists; teacher/lesson metadata and memory expectancy bias remain proposal annotations and score bias, not direct action injection | Teacher metadata priority bypass or one-byte public motor token | P15 can integrate this reference into the CPU brain tick loop |
| 2026-06-11 | P10 | Seal runtime `ExperiencePatch` records through a builder instead of exposing partially constructible patches | Learning, memory, topology, and logging consumers must only inspect complete causal events after pre-action, decision, and post-action data validates | Public mutable `ExperiencePatch` fields or accepting partial phase structs downstream | P11 consumes sealed patches for packed export; P12/P13/P15 consume sealed runtime views |
| 2026-06-11 | P10 | Keep memory expectancy in P10 as a bounded placeholder with no action replay fields | P12 owns memory recall; P10 only needs the causal slot and must preserve memory-as-expectancy, not old action injection | Adding replayed action IDs or implementing memory retrieval in P10 | P12 replaces/extends the placeholder through the sealed pre-action context |
| 2026-06-11 | P11 | Pack sealed `ExperiencePatch` records into fixed lossy frames plus deterministic side buffers | High-volume logs need stable bounded metadata without contaminating rich runtime cognition structs or inlining variable payloads | Using `ExperiencePatch` as the packed format, adding `Vec` fields to fixed frames, or accepting unsealed builder state | P30 can build offline tooling on the P11 schema; P34 owns future migration handling |
| 2026-06-12 | P12 | Store episodic memories in bounded deterministic banks and recall expectancy bias, not actions | Memory must influence interpretation and salience while preserving action arbitration as the only command output path | Returning historical selected actions or exposing an action replay field on `MemoryExpectancy` | P15 can query expectancy before decision; P16 can compress/merge records through the consolidation interface |
| 2026-06-12 | P13 | Keep the topological concept map as a bounded CPU-side ledger over sealed patches | P13 must provide concepts, relations, simplexes, unresolved gaps, and curiosity bias without introducing graph databases, GPU graph structures, engine types, or action-command bypasses | Graph database-style storage, unbounded concept growth, direct motor command output from topology, or renderer/engine references in core | P15 can call the map after patch sealing; P16 can later consolidate/simplify gaps and simplexes during sleep |
| 2026-06-11 | P14 | Make dense 16x16 and COO the first executable sparse tile formats, with row/column variants explicit but unsupported | P14 needs a deterministic CPU oracle now while preserving schema room for later GPU/storage variants | Implementing every sparse format before parity fixtures, or omitting row/column variants from the public schema | P24 can translate the same enum into GPU buffer contracts; unsupported variants must gain parity tests before execution |
| 2026-06-11 | P14 | Keep CPU neural state as per-creature linear activation/accumulator buffers plus sparse projection payloads | The performance contract allows O(N) activation state but forbids dense N x N runtime weight matrices in core | Allocating dense runtime matrices or assuming one monolithic multi-GB buffer | P15 should build the brain tick loop on this oracle without adding dense allocation assumptions |
| 2026-06-12 | P12/P13/P14 integration | Remove action-ID hints from topology curiosity output | Curiosity should bias salience/expectancy through arbitration inputs, not expose a public action-selection shortcut | Keeping an always-empty `action_id_hint` field on `CuriosityBias` | P15 should consume curiosity as bounded salience/voltage metadata only |
| 2026-06-12 | P15 | Stage post-seal CPU reference updates on cloned memory, topology, neural, and homeostatic state before committing them | P15 must prove no learning or state mutation occurs from invalid, partial, or unsealed experience while still wiring the first deterministic single-creature tick | Mutating memory/topology immediately after partial phase construction, or rolling back ad hoc after failures | P16/P17 can build on the same sealed-patch sequencing without changing the safety rule |
| 2026-06-12 | P15 | Use deterministic fixture proposals as the interim neural proposal source while still running P14 CPU neural state hooks | P14 exposes the sparse CPU oracle but not a mature motor proposal generator; P15 needs exact tick order without inventing hidden neural runtime behavior | Pretending fixture proposals are final neural runtime, or implementing a full proposal-generation kernel in P15 | P24/P15 follow-ups can replace fixtures with neural-derived proposals behind the same tick interface |
| 2026-06-12 | P16 | Keep sleep consolidation as a deterministic CPU sleep/offline phase with staged structural edit batches | P16 must stabilize lifetime learning and cleanup without active tick matrix resizing or GPU recompaction work | Applying structural edits during active tick, mutating sparse projection topology immediately, or hiding GPU recompaction in core | P17 can drive sleep through a harness; P28 owns future recompaction/application |
| 2026-06-12 | P16 | Preserve genotype/phenotype separation during H-trace drain and lifetime trait promotion | Lifetime habits may update `WLifetimeConsolidated`, `HOperational`, and `HShadow`, but inherited genetic priors must remain immutable unless explicit future Lamarckian mode opts in | Baking stable lifetime traces into `WGeneticFixed` by default or treating Lamarckian inheritance as normal consolidation | P33 can implement experimental inheritance modes with separate opt-in tests |
| 2026-06-13 | P17 | Keep the headless harness world-owned and adapter-driven rather than adding world behavior to core | P17 needs realistic deterministic behavior loops while preserving `alife_core` as pure contracts and the P15 brain tick as the sequencing boundary | Adding Bevy/renderer objects, engine entities, or world simulation dependencies to core; bypassing P09 arbitration with topology or memory | P18 can add richer scripted scenarios on top of the harness; P19 can freeze golden traces; P20 can benchmark the same API |
| 2026-06-13 | P18 | Keep scenario fixtures declarative and assert directionality instead of exact neural scores | P18 must prove food, pain, obstacle, sleep, curiosity, word, social, and teacher perception loops work while leaving exact broad traces to P19 and benchmark thresholds to P20 | Hardcoding P19 golden traces, adding hidden teacher/SLM state, or implementing P23 school runtime in the scenario suite | P19 can freeze stable summaries as golden traces; P20 can benchmark the same scenario runner; P23 can build teacher curriculum runtime on perceptual channels |
| 2026-06-13 | P24 | Encode GPU upload contracts explicitly as little-endian page-relative records | P24 freezes shader-visible layout before real compute passes, and explicit serialization avoids unsafe POD assumptions while preserving deterministic CPU fixture conversion | Raw host pointers, unsafe transmute/bytemuck packing for new structs, monolithic buffer offsets, or letting future shaders invent parallel layouts | P25/P26 must consume these records for parity work; P27/P28 may extend culling/recompaction without changing active tick pointer rules |
