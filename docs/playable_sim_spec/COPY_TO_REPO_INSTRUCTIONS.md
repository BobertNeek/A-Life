# Copy Instructions

Copy this directory into the repository as:

`docs/playable_sim_spec/`

Then commit the spec import or let G00 import/verify it. The first implementation prompt is `prompts/NEXT_PROMPT_G00.md`.
