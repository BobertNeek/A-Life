# Codex Master Prompt - Playable Sim Phase

Read `docs/playable_sim_spec/README.md`, `GLOBAL_INVARIANTS.md`, `VALIDATION_PROTOCOL.md`, `EXECUTION_ORDER.md`, and the assigned plan before editing.

Execute exactly one assigned G-plan unless the user explicitly provides the Goal Mode driver prompt. Never create P37. Preserve the P36 release-gate baseline. Use Windows PowerShell validation wrappers on Windows.

Every implementation branch must: audit prerequisites, implement only the assigned scope, add focused tests, run validation, update progress/docs, produce the required receipt, and stop.
