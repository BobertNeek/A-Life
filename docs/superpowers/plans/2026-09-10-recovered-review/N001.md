# N001 — P1: Newborn body initialization can fail its own bit-exact validator implementation plan

Goal: fix N001 only. Use superpowers:executing-plans in its smallest form. The user's no-build and minimal-test constraints override conflicting skill steps.

Read README.md beside this packet for shared constraints. Architecture: existing Rust authority and contracts remain in place. This is an implementation repair, not an architecture revision.

Lane: body. Dependencies: none. Assigned starting SHA comes from the supervisor dispatch.

## Exact initial scope

- Modify: `crates/alife_core/src/biochemistry.rs`
- Optional one regression: `crates/alife_core/tests/biochemistry_development.rs`

Other source files may be read. If correctness needs changes outside this scope, report the smallest exact expansion to the supervisor; do not make a half-fix.

## Evidence to verify

BodyState::baseline assigns the same scalar energy to six organs and to the compatibility field. Validation recomputes that field as a floating-point sum divided by six and demands identical bits. With metabolic_efficiency=f32(0.8), construction produces 0x3f666666 while sequential recomputation produces 0x3f666667. BiochemistryState::new_with_age immediately validates the result. Legacy body migration uses the same inconsistent initialization pattern.

Snapshot locations, relocate symbols in current code: `crates/alife_core/src/biochemistry.rs:114–143`; `crates/alife_core/src/biochemistry.rs:248–320`; `crates/alife_core/src/biochemistry.rs:563–603`.

Full supplied evidence remains at C:\Users\PC\Downloads\alife-recovered-evidence-7f3eea17.md under n001. Do not load the whole file.

## Steps

- [ ] Check assigned HEAD and status. Confirm this mechanism still exists and find the direct callers.
- [ ] Implement the smallest causal repair: Use refresh_compatibility_projections in baseline and legacy scalar migration after constructing organ state. Keep exact bit validation. Cover efficiency 0.8 and one legacy migration in one table test.
- [ ] Add one compact regression where practical. Acceptance scenario: Sweep legal metabolic-efficiency values through birth and test representative legacy scalar migrations; require successful self-validation and stable round trips.
- [ ] Read the complete diff and changed callers. Run only the allowed static checks. Record Rust regression as UNRUN, not passed.
- [ ] Stage exact intended files and commit with N001 in the subject. Return a concise handoff for the supervisor's manual review. Do not claim integration or runtime verification.

