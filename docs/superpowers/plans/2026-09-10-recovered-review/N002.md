# N002 — P1: Cognitive-energy accounting flattens organ-local energy state implementation plan

Goal: fix N002 only. Use superpowers:executing-plans in its smallest form. The user's no-build and minimal-test constraints override conflicting skill steps.

Read README.md beside this packet for shared constraints. Architecture: existing Rust authority and contracts remain in place. This is an implementation repair, not an architecture revision.

Lane: body. Dependencies: N001. Assigned starting SHA comes from the supervisor dispatch.

## Exact initial scope

- Modify: `crates/alife_core/src/biochemistry.rs`
- Modify: `crates/alife_world/src/organism.rs`
- Optional one regression: `crates/alife_world/tests/world_organism_registry.rs`

Other source files may be read. If correctness needs changes outside this scope, report the smallest exact expansion to the supervisor; do not make a half-fix.

## Evidence to verify

OrganismRecord::account_cognitive_work subtracts the cognitive debit from mean body energy and invokes BodyState::set_energy. That setter overwrites every organ reserve with the same scalar. Heterogeneous reserves are homogenized even when the applied debit is zero.

Snapshot locations, relocate symbols in current code: `crates/alife_world/src/organism.rs:600–633`; `crates/alife_core/src/biochemistry.rs:225–233`; `crates/alife_game_app/src/gpu_live_runtime.rs:4096–4113`.

Full supplied evidence remains at C:\Users\PC\Downloads\alife-recovered-evidence-7f3eea17.md under n002. Do not load the whole file.

## Steps

- [ ] Check assigned HEAD and status. Confirm this mechanism still exists and find the direct callers.
- [ ] Implement the smallest causal repair: Choose an explicit local-reserve debit policy consistent with existing energy units. Zero debit must return without redistributing energy. Preserve heterogeneity under positive debit, clamp at zero, and refresh compatibility projections. Do not change set_energy globally if other callers need its initialization semantics.
- [ ] Add one compact regression where practical. Acceptance scenario: Create a valid heterogeneous body, apply zero and nonzero work charges, and check local reserves and the projected total against the declared debit policy.
- [ ] Read the complete diff and changed callers. Run only the allowed static checks. Record Rust regression as UNRUN, not passed.
- [ ] Stage exact intended files and commit with N002 in the subject. Return a concise handoff for the supervisor's manual review. Do not claim integration or runtime verification.

