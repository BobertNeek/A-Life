# N003 — P2: Organ upkeep is tied to destination divisibility rather than elapsed cadence crossings implementation plan

Goal: fix N003 only. Use superpowers:executing-plans in its smallest form. The user's no-build and minimal-test constraints override conflicting skill steps.

Read README.md beside this packet for shared constraints. Architecture: existing Rust authority and contracts remain in place. This is an implementation repair, not an architecture revision.

Lane: body. Dependencies: N002. Assigned starting SHA comes from the supervisor dispatch.

## Exact initial scope

- Modify: `crates/alife_core/src/biochemistry.rs`
- Optional one regression: `crates/alife_core/tests/biochemistry_development.rs`

Other source files may be read. If correctness needs changes outside this scope, report the smallest exact expansion to the supervisor; do not make a half-fix.

## Evidence to verify

BodyState::apply_event charges an organ once when next_tick is divisible by its cadence. It has no prior-tick argument. For cadence six, 5→7 misses the maintenance at tick six, while 5→6→7 charges it. Repeating a same-timestamp body update at a cadence boundary can charge again.

Snapshot locations, relocate symbols in current code: `crates/alife_core/src/biochemistry.rs:146–198`; `crates/alife_core/src/biochemistry.rs:639–668`; `docs/architecture/ALife_Complete_Organism_and_Intelligence_Architecture_v2.0_CONTROLLING.md:236–248`.

Full supplied evidence remains at C:\Users\PC\Downloads\alife-recovered-evidence-7f3eea17.md under n003. Do not load the whole file.

## Steps

- [ ] Check assigned HEAD and status. Confirm this mechanism still exists and find the direct callers.
- [ ] Implement the smallest causal repair: Pass previous tick into body event application, count crossed organ boundaries with bounded arithmetic, and separate periodic upkeep from instantaneous events. Trace all callers of any changed signature.
- [ ] Add one compact regression where practical. Acceptance scenario: Compare equivalent partitioned intervals, including a boundary inside the interval and a repeated same-tick zero-event call.
- [ ] Read the complete diff and changed callers. Run only the allowed static checks. Record Rust regression as UNRUN, not passed.
- [ ] Stage exact intended files and commit with N003 in the subject. Return a concise handoff for the supervisor's manual review. Do not claim integration or runtime verification.

