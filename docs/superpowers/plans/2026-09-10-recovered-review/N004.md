# N004 — P2: Reactions execute for one unit of time when elapsed time is zero implementation plan

Goal: fix N004 only. Use superpowers:executing-plans in its smallest form. The user's no-build and minimal-test constraints override conflicting skill steps.

Read README.md beside this packet for shared constraints. Architecture: existing Rust authority and contracts remain in place. This is an implementation repair, not an architecture revision.

Lane: graph. Dependencies: N008. Assigned starting SHA comes from the supervisor dispatch.

## Exact initial scope

- Modify: `crates/alife_core/src/biochemical_graph.rs`
- Optional one regression: `crates/alife_core/tests/biochemical_graph_v2.rs`

Other source files may be read. If correctness needs changes outside this scope, report the smallest exact expansion to the supervisor; do not make a half-fix.

## Evidence to verify

BiochemicalGraphState::advance computes elapsed=next_tick-current_tick, then passes elapsed.max(1.0)*developmental_expression into every reaction. Same-timestamp calls can therefore consume substrate and create products even without a timed interval or event.

Snapshot locations, relocate symbols in current code: `crates/alife_core/src/biochemical_graph.rs:731–757`; `crates/alife_core/src/biochemical_graph.rs:807–825`; `crates/alife_core/src/biochemical_graph.rs:973–1003`.

Full supplied evidence remains at C:\Users\PC\Downloads\alife-recovered-evidence-7f3eea17.md under n004. Do not load the whole file.

## Steps

- [ ] Check assigned HEAD and status. Confirm this mechanism still exists and find the direct callers.
- [ ] Implement the smallest causal repair: Remove invented kinetic duration on same-tick calls. Preserve explicit instantaneous events, contract checks, and existing conservation solver.
- [ ] Add one compact regression where practical. Acceptance scenario: A valid active reaction with no event and no elapsed time must leave concentrations unchanged.
- [ ] Read the complete diff and changed callers. Run only the allowed static checks. Record Rust regression as UNRUN, not passed.
- [ ] Stage exact intended files and commit with N004 in the subject. Return a concise handoff for the supervisor's manual review. Do not claim integration or runtime verification.

