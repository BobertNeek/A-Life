# N005 — P2: Emitter catch-up collapses several cadence crossings into one release implementation plan

Goal: fix N005 only. Use superpowers:executing-plans in its smallest form. The user's no-build and minimal-test constraints override conflicting skill steps.

Read README.md beside this packet for shared constraints. Architecture: existing Rust authority and contracts remain in place. This is an implementation repair, not an architecture revision.

Lane: graph. Dependencies: N004. Assigned starting SHA comes from the supervisor dispatch.

## Exact initial scope

- Modify: `crates/alife_core/src/biochemical_graph.rs`
- Optional one regression: `crates/alife_core/tests/biochemical_graph_v2.rs`

Other source files may be read. If correctness needs changes outside this scope, report the smallest exact expansion to the supervisor; do not make a half-fix.

## Evidence to verify

crossed_cadence returns only a boolean. Each emitter applies its delta once if any boundary was crossed. A constant unsaturated cadence-one emitter releases once for 0→2 but twice for 0→1→2.

Snapshot locations, relocate symbols in current code: `crates/alife_core/src/biochemical_graph.rs:759–785`; `crates/alife_core/src/biochemical_graph.rs:1045–1048`; `docs/architecture/ALife_Complete_Organism_and_Intelligence_Architecture_v2.0_CONTROLLING.md:236–250`; `docs/architecture/ALife_Complete_Organism_and_Intelligence_Architecture_v2.0_CONTROLLING.md:413–419`.

Full supplied evidence remains at C:\Users\PC\Downloads\alife-recovered-evidence-7f3eea17.md under n005. Do not load the whole file.

## Steps

- [ ] Check assigned HEAD and status. Confirm this mechanism still exists and find the direct callers.
- [ ] Implement the smallest causal repair: Replace boolean cadence crossing with bounded crossing count for basal unsaturated emission. Account explicitly for source-reset emitters so the source is not repeatedly reused. Preserve documented catch-up limits.
- [ ] Add one compact regression where practical. Acceptance scenario: Use a basal emitter below saturation and compare a multi-boundary interval against its single-boundary partition.
- [ ] Read the complete diff and changed callers. Run only the allowed static checks. Record Rust regression as UNRUN, not passed.
- [ ] Stage exact intended files and commit with N005 in the subject. Return a concise handoff for the supervisor's manual review. Do not claim integration or runtime verification.

