# N008 — P2: Chemical species ranges and generic graph validation disagree implementation plan

Goal: fix N008 only. Use superpowers:executing-plans in its smallest form. The user's no-build and minimal-test constraints override conflicting skill steps.

Read README.md beside this packet for shared constraints. Architecture: existing Rust authority and contracts remain in place. This is an implementation repair, not an architecture revision.

Lane: graph. Dependencies: N007. Assigned starting SHA comes from the supervisor dispatch.

## Exact initial scope

- Modify: `crates/alife_core/src/biochemical_graph.rs`
- Modify: `crates/alife_core/src/biochemistry.rs`
- Optional one regression: `crates/alife_core/tests/biochemical_graph_v2.rs`

Other source files may be read. If correctness needs changes outside this scope, report the smallest exact expansion to the supervisor; do not make a half-fix.

## Evidence to verify

ChemicalSpecies accepts finite nonnegative ranges such as minimum=1, baseline=2, maximum=3. BiochemicalGraphState::new initializes from that baseline and validate_against accepts the declared range, but its generic Validate implementation hard-codes every active concentration to [0,1]. The owning BiochemistryState invokes that generic validator.

Snapshot locations, relocate symbols in current code: `crates/alife_core/src/biochemical_graph.rs:69–85`; `crates/alife_core/src/biochemical_graph.rs:697–713`; `crates/alife_core/src/biochemical_graph.rs:898–934`; `crates/alife_core/src/biochemistry.rs:735–750`; `docs/architecture/ALife_Complete_Organism_and_Intelligence_Architecture_v2.0_CONTROLLING.md:409–419`.

Full supplied evidence remains at C:\Users\PC\Downloads\alife-recovered-evidence-7f3eea17.md under n008. Do not load the whole file.

## Steps

- [ ] Check assigned HEAD and status. Confirm this mechanism still exists and find the direct callers.
- [ ] Implement the smallest causal repair: Generic validation must enforce structural finite nonnegative state, not invented unit bounds. Keep phenotype-bound species range validation at constructors, advance, and restore callers. Trace callers before weakening any check.
- [ ] Add one compact regression where practical. Acceptance scenario: Construct, advance, save and restore a legal non-unit-range species and check that every validator agrees.
- [ ] Read the complete diff and changed callers. Run only the allowed static checks. Record Rust regression as UNRUN, not passed.
- [ ] Stage exact intended files and commit with N008 in the subject. Return a concise handoff for the supervisor's manual review. Do not claim integration or runtime verification.


## Supervisor scope extension

Authorized 2026-09-11: also modify crates/alife_world/src/organism.rs, crates/alife_game_app/src/curated_founder_materializer.rs, and crates/alife_game_app/src/curated_founder_staging.rs only to wire phenotype-bound biochemistry validation at existing owner/admission checks. Preserve identity/error mapping. One regression total and no Cargo/build/GPU remain in force.
