# N009 — P3: Neuroemitter work accounting counts input emissions, not evaluated emitters implementation plan

Goal: fix N009 only. Use superpowers:executing-plans in its smallest form. The user's no-build and minimal-test constraints override conflicting skill steps.

Read README.md beside this packet for shared constraints. Architecture: existing Rust authority and contracts remain in place. This is an implementation repair, not an architecture revision.

Lane: graph. Dependencies: N005. Assigned starting SHA comes from the supervisor dispatch.

## Exact initial scope

- Modify: `crates/alife_core/src/biochemical_graph.rs`
- Optional one regression: `crates/alife_core/tests/biochemical_graph_v2.rs`

Other source files may be read. If correctness needs changes outside this scope, report the smallest exact expansion to the supervisor; do not make a half-fix.

## Evidence to verify

The code loops over phenotype.neuroemitters, scanning neural emissions for each. It records frame.emissions.len() as neural_emitter_evaluations instead of the number of configured neuroemitters evaluated.

Snapshot locations, relocate symbols in current code: `crates/alife_core/src/biochemical_graph.rs:787–805`; `crates/alife_core/src/biochemical_graph.rs:818–824`.

Full supplied evidence remains at C:\Users\PC\Downloads\alife-recovered-evidence-7f3eea17.md under n009. Do not load the whole file.

## Steps

- [ ] Check assigned HEAD and status. Confirm this mechanism still exists and find the direct callers.
- [ ] Implement the smallest causal repair: Report configured neuroemitters actually evaluated when a frame exists. Do not add a new accounting subsystem.
- [ ] Add one compact regression where practical. Acceptance scenario: Vary emitter count and input-frame length independently and verify each counter tracks its named operation.
- [ ] Read the complete diff and changed callers. Run only the allowed static checks. Record Rust regression as UNRUN, not passed.
- [ ] Stage exact intended files and commit with N009 in the subject. Return a concise handoff for the supervisor's manual review. Do not claim integration or runtime verification.

