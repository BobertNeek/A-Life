# N010 — P1: Motor-driven biology bypasses ambient mating-opportunity injection implementation plan

Goal: fix N010 only. Use superpowers:executing-plans in its smallest form. The user's no-build and minimal-test constraints override conflicting skill steps.

Read README.md beside this packet for shared constraints. Architecture: existing Rust authority and contracts remain in place. This is an implementation repair, not an architecture revision.

Lane: world. Dependencies: N019. Assigned starting SHA comes from the supervisor dispatch.

## Exact initial scope

- Modify: `crates/alife_world/src/headless.rs`
- Optional one regression: `crates/alife_world/tests/headless_world_harness.rs`

Other source files may be read. If correctness needs changes outside this scope, report the smallest exact expansion to the supervisor; do not make a half-fix.

## Evidence to verify

The registered motor transaction advances organism biology to outcome_tick using action/hazard events. The later world advancement injects ambient mating_opportunity only for organisms still at current_tick, and skips those already at next_tick. The reproduced control-flow path therefore misses the environmental mating signal for ordinary awake actors.

Snapshot locations, relocate symbols in current code: `crates/alife_world/src/headless.rs:654–695`; `crates/alife_world/src/headless.rs:2240–2257`; `crates/alife_world/src/headless.rs:2287–2315`; `crates/alife_core/src/biochemistry.rs:519–524`; `crates/alife_core/src/biochemistry.rs:691–702`.

Full supplied evidence remains at C:\Users\PC\Downloads\alife-recovered-evidence-7f3eea17.md under n010. Do not load the whole file.

## Steps

- [ ] Check assigned HEAD and status. Confirm this mechanism still exists and find the direct callers.
- [ ] Implement the smallest causal repair: Share ambient mating-opportunity calculation between motor and ordinary biology advancement. Merge it before exactly one advance. Retain action/hazard effects and transaction rollback.
- [ ] Add one compact regression where practical. Acceptance scenario: Use mature hormone-ready nearby actors executing ordinary neural motor bundles across a reproduction boundary; compare ambient opportunity against nonacting controls.
- [ ] Read the complete diff and changed callers. Run only the allowed static checks. Record Rust regression as UNRUN, not passed.
- [ ] Stage exact intended files and commit with N010 in the subject. Return a concise handoff for the supervisor's manual review. Do not claim integration or runtime verification.

