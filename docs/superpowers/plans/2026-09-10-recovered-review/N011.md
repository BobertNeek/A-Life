# N011 — P2: Different parental cadence phases can prevent natural reproduction indefinitely implementation plan

Goal: fix N011 only. Use superpowers:executing-plans in its smallest form. The user's no-build and minimal-test constraints override conflicting skill steps.

Read README.md beside this packet for shared constraints. Architecture: existing Rust authority and contracts remain in place. This is an implementation repair, not an architecture revision.

Lane: world. Dependencies: N010. Assigned starting SHA comes from the supervisor dispatch.

## Exact initial scope

- Modify: `crates/alife_world/src/headless.rs`
- Modify: `crates/alife_core/src/biochemistry.rs`
- Optional one regression: `crates/alife_world/tests/headless_world_harness.rs`

Other source files may be read. If correctness needs changes outside this scope, report the smallest exact expansion to the supervisor; do not make a half-fix.

## Evidence to verify

Natural conception requires each cached reproduction.last_update_tick to equal that parent’s current biological age. These stamps are cadence boundaries. At the reference period of 120 ticks, parents born one tick apart cannot both be exactly at a fresh boundary in the same world tick.

Snapshot locations, relocate symbols in current code: `crates/alife_world/src/headless.rs:734–755`; `crates/alife_core/src/biochemistry.rs:22–29`; `crates/alife_core/src/biochemistry.rs:655–660`; `crates/alife_core/src/biochemistry.rs:691–701`; `crates/alife_core/src/biochemistry.rs:773–775`.

Full supplied evidence remains at C:\Users\PC\Downloads\alife-recovered-evidence-7f3eea17.md under n011. Do not load the whole file.

## Steps

- [ ] Check assigned HEAD and status. Confirm this mechanism still exists and find the direct callers.
- [ ] Implement the smallest causal repair: Validate readiness within its cadence interval, and recheck present eligibility and biology freshness. Do not simply remove timestamp checks or allow indefinite stale readiness. Cover one-tick parental age offsets.
- [ ] Add one compact regression where practical. Acceptance scenario: Test otherwise ready pairs with different birth-tick residues through multiple reproductive periods.
- [ ] Read the complete diff and changed callers. Run only the allowed static checks. Record Rust regression as UNRUN, not passed.
- [ ] Stage exact intended files and commit with N011 in the subject. Return a concise handoff for the supervisor's manual review. Do not claim integration or runtime verification.

