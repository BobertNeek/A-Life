# N012 — P2: Production retained-history mode observes the same sealed patch twice implementation plan

Goal: fix N012 only. Use superpowers:executing-plans in its smallest form. The user's no-build and minimal-test constraints override conflicting skill steps.

Read README.md beside this packet for shared constraints. Architecture: existing Rust authority and contracts remain in place. This is an implementation repair, not an architecture revision.

Lane: runtime. Dependencies: N013. Assigned starting SHA comes from the supervisor dispatch.

## Exact initial scope

- Modify: `crates/alife_game_app/src/gpu_live_runtime.rs`
- Optional one inline regression in the existing module test area.

Other source files may be read. If correctness needs changes outside this scope, report the smallest exact expansion to the supervisor; do not make a half-fix.

## Evidence to verify

Finalization appends each patch to sealed_patches and updates last_sealed_patches. observe_passive_tick concatenates the retained tail and the recent list without deduplication, then observes both copies when their ticks match. Food/hazard/language-derived exposures can be counted twice while survival ticks are counted once.

Snapshot locations, relocate symbols in current code: `crates/alife_game_app/src/gpu_live_runtime.rs:1540–1547`; `crates/alife_game_app/src/gpu_live_runtime.rs:7952–7993`; `crates/alife_game_app/src/gpu_live_runtime.rs:8495–8524`; `crates/alife_game_app/src/gpu_live_runtime/staged_tick.rs:815–827`.

Full supplied evidence remains at C:\Users\PC\Downloads\alife-recovered-evidence-7f3eea17.md under n012. Do not load the whole file.

## Steps

- [ ] Check assigned HEAD and status. Confirm this mechanism still exists and find the direct callers.
- [ ] Implement the smallest causal repair: Observe one canonical collection of each tick's committed patches exactly once regardless of history retention setting. Do not rely solely on retention being disabled.
- [ ] Add one compact regression where practical. Acceptance scenario: Run identical sealed outcomes with retention enabled and disabled; all passive metrics must be identical.
- [ ] Read the complete diff and changed callers. Run only the allowed static checks. Record Rust regression as UNRUN, not passed.
- [ ] Stage exact intended files and commit with N012 in the subject. Return a concise handoff for the supervisor's manual review. Do not claim integration or runtime verification.

