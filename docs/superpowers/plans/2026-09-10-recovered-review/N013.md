# N013 — P1: Production retains full lifetime ExperiencePatch history in unbounded memory implementation plan

Goal: fix N013 only. Use superpowers:executing-plans in its smallest form. The user's no-build and minimal-test constraints override conflicting skill steps.

Read README.md beside this packet for shared constraints. Architecture: existing Rust authority and contracts remain in place. This is an implementation repair, not an architecture revision.

Lane: runtime. Dependencies: none. Assigned starting SHA comes from the supervisor dispatch.

## Exact initial scope

- Modify: `crates/alife_game_app/src/gpu_live_runtime.rs`
- Optional one inline regression in the existing module test area.

Other source files may be read. If correctness needs changes outside this scope, report the smallest exact expansion to the supervisor; do not make a half-fix.

## Evidence to verify

Production and ordinary restore enable retain_sealed_patch_history. Each committed batch extends the history Vec with full cloned patches; no active-path eviction was found. Memory therefore scales with accumulated decisions, not only bounded active cognition. The soak restore explicitly turns retention off.

Snapshot locations, relocate symbols in current code: `crates/alife_game_app/src/gpu_live_runtime.rs:1531–1549`; `crates/alife_game_app/src/gpu_live_runtime.rs:5528–5540`; `crates/alife_game_app/src/gpu_live_runtime.rs:5843–5863`; `crates/alife_game_app/src/gpu_live_runtime.rs:8495–8524`.

Full supplied evidence remains at C:\Users\PC\Downloads\alife-recovered-evidence-7f3eea17.md under n013. Do not load the whole file.

## Steps

- [ ] Check assigned HEAD and status. Confirm this mechanism still exists and find the direct callers.
- [ ] Implement the smallest causal repair: Bound or disable optional full review-history retention consistently in production and ordinary restore. Trace all sealed_patches consumers, pending replay, checkpoint, diagnostic and assessment paths before changing retention. Never truncate acquired cognition or pending transactions. Avoid a long soak; add one focused source-level regression if practical.
- [ ] Add one compact regression where practical. Acceptance scenario: Use production-equivalent retention in a long-run memory test, and verify bounded memory while exact continuation and outstanding replay remain intact.
- [ ] Read the complete diff and changed callers. Run only the allowed static checks. Record Rust regression as UNRUN, not passed.
- [ ] Stage exact intended files and commit with N013 in the subject. Return a concise handoff for the supervisor's manual review. Do not claim integration or runtime verification.

