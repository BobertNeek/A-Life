# N014 — P3: Passive exposure records always label the environment Temperate implementation plan

Goal: fix N014 only. Use superpowers:executing-plans in its smallest form. The user's no-build and minimal-test constraints override conflicting skill steps.

Read README.md beside this packet for shared constraints. Architecture: existing Rust authority and contracts remain in place. This is an implementation repair, not an architecture revision.

Lane: runtime. Dependencies: N012. Assigned starting SHA comes from the supervisor dispatch.

## Exact initial scope

- Modify: `crates/alife_game_app/src/gpu_live_runtime.rs`
- Optional one inline regression in the existing module test area.

Other source files may be read. If correctness needs changes outside this scope, report the smallest exact expansion to the supervisor; do not make a half-fix.

## Evidence to verify

observe_passive_tick supplies EnvironmentalRegime::Temperate unconditionally. The statistics record accepts and accumulates that label as measured regime exposure.

Snapshot locations, relocate symbols in current code: `crates/alife_game_app/src/gpu_live_runtime.rs:7981–7993`; `crates/alife_core/src/evaluation.rs:287–327`.

Full supplied evidence remains at C:\Users\PC\Downloads\alife-recovered-evidence-7f3eea17.md under n014. Do not load the whole file.

## Steps

- [ ] Check assigned HEAD and status. Confirm this mechanism still exists and find the direct callers.
- [ ] Implement the smallest causal repair: Trace authoritative environment regime data. Use it if available; otherwise report exposures as Unknown using an existing representation and describe any needed cross-crate contract change before expanding scope. Never invent climate labels.
- [ ] Add one compact regression where practical. Acceptance scenario: Check a non-temperate scenario against the regime counts instead of only the default world.
- [ ] Read the complete diff and changed callers. Run only the allowed static checks. Record Rust regression as UNRUN, not passed.
- [ ] Stage exact intended files and commit with N014 in the subject. Return a concise handoff for the supervisor's manual review. Do not claim integration or runtime verification.

## Supervisor scope extension

Authorized: crates/alife_core/src/evaluation.rs and optionally one regression in crates/alife_core/tests/life_evaluation.rs. Introduce truthful unknown regime exposure while preserving six serialized known bins and historical records. Derive unknown count from survival minus checked known sum; retain survival/energy/movement/GPU accounting. Review compatibility explicitly. No schema array expansion, historical relabeling, Cargo or runtime tests.
