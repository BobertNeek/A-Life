# N016 — P2: Passive avoidance rates receive failure observations but no successful opportunities implementation plan

Goal: fix N016 only. Use superpowers:executing-plans in its smallest form. The user's no-build and minimal-test constraints override conflicting skill steps.

Read README.md beside this packet for shared constraints. Architecture: existing Rust authority and contracts remain in place. This is an implementation repair, not an architecture revision.

Lane: evaluation. Dependencies: N015. Assigned starting SHA comes from the supervisor dispatch.

## Exact initial scope

- Modify: `crates/alife_core/src/evaluation.rs`
- Optional one regression: `crates/alife_core/tests/life_evaluation.rs`

Other source files may be read. If correctness needs changes outside this scope, report the smallest exact expansion to the supervisor; do not make a half-fix.

## Evidence to verify

The ordinary sealed-patch observer emits PoisonEncounter{avoided:false} and HazardEncounter{avoided:false} after harmful outcomes. It does not record avoided=true opportunities. The resulting live readings are unknown before an incident, then zero from failure-only samples, regardless of successful avoidance behavior not observed.

Snapshot locations, relocate symbols in current code: `crates/alife_core/src/evaluation.rs:402–415`; `crates/alife_core/src/evaluation.rs:469–471`; `crates/alife_tools/src/era1_evolution.rs:275–295`.

Full supplied evidence remains at C:\Users\PC\Downloads\alife-recovered-evidence-7f3eea17.md under n016. Do not load the whole file.

## Steps

- [ ] Check assigned HEAD and status. Confirm this mechanism still exists and find the direct callers.
- [ ] Implement the smallest causal repair: Do not report avoidance rates from injury-only samples. Preserve actual incident accounting and leave avoidance Unknown unless an explicit grounded opportunity/outcome is recorded. Trace explicit encounter event API and direct statistics consumers.
- [ ] Add one compact regression where practical. Acceptance scenario: Provide known successful and failed opportunities in the passive path and verify denominators; do not fabricate opportunities from absence of injury.
- [ ] Read the complete diff and changed callers. Run only the allowed static checks. Record Rust regression as UNRUN, not passed.
- [ ] Stage exact intended files and commit with N016 in the subject. Return a concise handoff for the supervisor's manual review. Do not claim integration or runtime verification.

