# N017 — P2: Sealed-patch observation can mutate finalized statistics and partially mutate on rejection implementation plan

Goal: fix N017 only. Use superpowers:executing-plans in its smallest form. The user's no-build and minimal-test constraints override conflicting skill steps.

Read README.md beside this packet for shared constraints. Architecture: existing Rust authority and contracts remain in place. This is an implementation repair, not an architecture revision.

Lane: evaluation. Dependencies: none. Assigned starting SHA comes from the supervisor dispatch.

## Exact initial scope

- Modify: `crates/alife_core/src/evaluation.rs`
- Optional one regression: `crates/alife_core/tests/life_evaluation.rs`

Other source files may be read. If correctness needs changes outside this scope, report the smallest exact expansion to the supervisor; do not make a half-fix.

## Evidence to verify

observe rejects events after death, but observe_sealed_patch increments heard_token_exposures before reaching any nested observe call and does not check death itself. A finalized record with an otherwise uneventful heard-token patch can be modified successfully; a patch requiring nested event recording can return an error after its heard count changed.

Snapshot locations, relocate symbols in current code: `crates/alife_core/src/evaluation.rs:287–293`; `crates/alife_core/src/evaluation.rs:380–434`; `crates/alife_core/src/evaluation.rs:437–452`.

Full supplied evidence remains at C:\Users\PC\Downloads\alife-recovered-evidence-7f3eea17.md under n017. Do not load the whole file.

## Steps

- [ ] Check assigned HEAD and status. Confirm this mechanism still exists and find the direct callers.
- [ ] Implement the smallest causal repair: Guard finalized lifecycle and stage patch observation on a copy, committing only after every fallible operation succeeds. Preserve fixed-size statistics, identity checks, and current schema.
- [ ] Add one compact regression where practical. Acceptance scenario: After finalize, every rejected patch must leave all fields unchanged; cover both an otherwise idle patch and one containing a food/hazard event.
- [ ] Read the complete diff and changed callers. Run only the allowed static checks. Record Rust regression as UNRUN, not passed.
- [ ] Stage exact intended files and commit with N017 in the subject. Return a concise handoff for the supervisor's manual review. Do not claim integration or runtime verification.

