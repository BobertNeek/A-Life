# N018 — P1: A selected Idle command is silently executed as Rest implementation plan

Goal: fix N018 only. Use superpowers:executing-plans in its smallest form. The user's no-build and minimal-test constraints override conflicting skill steps.

Read README.md beside this packet for shared constraints. Architecture: existing Rust authority and contracts remain in place. This is an implementation repair, not an architecture revision.

Lane: world. Dependencies: none. Assigned starting SHA comes from the supervisor dispatch.

## Exact initial scope

- Modify: `crates/alife_world/src/headless.rs`
- Optional one regression: `crates/alife_world/tests/headless_world_harness.rs`

Other source files may be read. If correctness needs changes outside this scope, report the smallest exact expansion to the supervisor; do not make a half-fix.

## Evidence to verify

Factorized arbitration places Idle in the Posture channel while preserving its primitive. legacy_action_for_motor_channel special-cases only Inspect and maps every other Posture command to Rest. The ordinary executor therefore applies OutcomeProfile::rest, including sleep_recovery=1, instead of the actual selected Idle semantics.

Snapshot locations, relocate symbols in current code: `crates/alife_core/src/factorized_selection.rs:137–156`; `crates/alife_world/src/candidate_enumerator.rs:209–237`; `crates/alife_world/src/headless.rs:4234–4247`; `crates/alife_world/src/headless.rs:2723–2738`; `crates/alife_world/src/headless.rs:4408–4429`.

Full supplied evidence remains at C:\Users\PC\Downloads\alife-recovered-evidence-7f3eea17.md under n018. Do not load the whole file.

## Steps

- [ ] Check assigned HEAD and status. Confirm this mechanism still exists and find the direct callers.
- [ ] Implement the smallest causal repair: Preserve Idle, Rest, and Inspect primitive identity through the adapter. Reject unsupported posture combinations using the existing error path. Check existing factorized output conventions so normal supported commands continue to work.
- [ ] Add one compact regression where practical. Acceptance scenario: Compare direct and factorized Idle/Rest/Inspect with identical pre-state; Idle must not acquire Rest’s biological recovery and the selected/effected primitives must agree.
- [ ] Read the complete diff and changed callers. Run only the allowed static checks. Record Rust regression as UNRUN, not passed.
- [ ] Stage exact intended files and commit with N018 in the subject. Return a concise handoff for the supervisor's manual review. Do not claim integration or runtime verification.

