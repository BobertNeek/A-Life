# N019 — P1: Grab and Inspect lack physical reach/ownership checks and can invent contact implementation plan

Goal: fix N019 only. Use superpowers:executing-plans in its smallest form. The user's no-build and minimal-test constraints override conflicting skill steps.

Read README.md beside this packet for shared constraints. Architecture: existing Rust authority and contracts remain in place. This is an implementation repair, not an architecture revision.

Lane: world. Dependencies: N018. Assigned starting SHA comes from the supervisor dispatch.

## Exact initial scope

- Modify: `crates/alife_world/src/headless.rs`
- Optional one regression: `crates/alife_world/tests/headless_world_harness.rs`

Other source files may be read. If correctness needs changes outside this scope, report the smallest exact expansion to the supervisor; do not make a half-fix.

## Evidence to verify

These actions use require_target, which checks existence only. Grab then assigns carried_by; Inspect reports Touch and can apply the target’s hazard damage without checking physical reach. Grounded candidate generation includes these actions for visible targets, so distant-target cases are not confined to arbitrary forged input.

Snapshot locations, relocate symbols in current code: `crates/alife_world/src/headless.rs:2739–2779`; `crates/alife_world/src/headless.rs:3064–3075`; `crates/alife_world/src/headless.rs:2373–2418`; `crates/alife_world/src/candidate_enumerator.rs:146–204`.

Full supplied evidence remains at C:\Users\PC\Downloads\alife-recovered-evidence-7f3eea17.md under n019. Do not load the whole file.

## Steps

- [ ] Check assigned HEAD and status. Confirm this mechanism still exists and find the direct callers.
- [ ] Implement the smallest causal repair: Enforce reach, target lifetime, self/ownership, mobility and available-effector legality for Grab. Visual Inspect may succeed remotely but must not create Touch or contact damage. Preserve legitimate nearby contact.
- [ ] Add one compact regression where practical. Acceptance scenario: Attempt distant Grab/Inspect, ownership theft and invalid objects; check no unauthorized ownership transfer or remote touch injury occurs.
- [ ] Read the complete diff and changed callers. Run only the allowed static checks. Record Rust regression as UNRUN, not passed.
- [ ] Stage exact intended files and commit with N019 in the subject. Return a concise handoff for the supervisor's manual review. Do not claim integration or runtime verification.

