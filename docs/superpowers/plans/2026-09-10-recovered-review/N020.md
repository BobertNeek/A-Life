# N020 — P2: A grabbed object is marked carried but does not follow its carrier implementation plan

Goal: fix N020 only. Use superpowers:executing-plans in its smallest form. The user's no-build and minimal-test constraints override conflicting skill steps.

Read README.md beside this packet for shared constraints. Architecture: existing Rust authority and contracts remain in place. This is an implementation repair, not an architecture revision.

Lane: world. Dependencies: N019, N011. Assigned starting SHA comes from the supervisor dispatch.

## Exact initial scope

- Modify: `crates/alife_world/src/headless.rs`
- Optional one regression: `crates/alife_world/tests/headless_world_harness.rs`

Other source files may be read. If correctness needs changes outside this scope, report the smallest exact expansion to the supervisor; do not make a half-fix.

## Evidence to verify

Grab stores carried_by, but execute_move updates the actor’s transform and velocity without attaching or relocating carried objects. The active carried_by references found handle assignment, identity/provenance, validation or serialization rather than motion attachment.

Snapshot locations, relocate symbols in current code: `crates/alife_world/src/headless.rs:2764–2779`; `crates/alife_world/src/headless.rs:2892–2962`; `crates/alife_world/src/headless.rs:4350–4364`.

Full supplied evidence remains at C:\Users\PC\Downloads\alife-recovered-evidence-7f3eea17.md under n020. Do not load the whole file.

## Steps

- [ ] Check assigned HEAD and status. Confirm this mechanism still exists and find the direct callers.
- [ ] Implement the smallest causal repair: Move held objects authoritatively with their carrier, preserve offset and ownership, and handle detach/death using existing lifecycle hooks. Existing serialized transforms should continue without introducing a new schema if unnecessary.
- [ ] Add one compact regression where practical. Acceptance scenario: Grab a nearby object, move, save/restore, and move again; verify object world position and ownership remain physically consistent.
- [ ] Read the complete diff and changed callers. Run only the allowed static checks. Record Rust regression as UNRUN, not passed.
- [ ] Stage exact intended files and commit with N020 in the subject. Return a concise handoff for the supervisor's manual review. Do not claim integration or runtime verification.

