# N021 — P2: Joint outcome aggregation loses independent contact/transfer results implementation plan

Goal: fix N021 only. Use superpowers:executing-plans in its smallest form. The user's no-build and minimal-test constraints override conflicting skill steps.

Read README.md beside this packet for shared constraints. Architecture: existing Rust authority and contracts remain in place. This is an implementation repair, not an architecture revision.

Lane: world. Dependencies: N023. Assigned starting SHA comes from the supervisor dispatch.

## Exact initial scope

- Modify: `crates/alife_world/src/headless.rs`
- Modify: `crates/alife_core/src/experience.rs`
- Optional one regression: `crates/alife_world/tests/headless_world_harness.rs`

Other source files may be read. If correctness needs changes outside this scope, report the smallest exact expansion to the supervisor; do not make a half-fix.

## Evidence to verify

Only locomotion produces a channel receipt. The joint physical summary retains one contact and target chosen by priority: Collision/Blocked outrank Consumed. A blocked movement plus successful ingestion can therefore lose the consumption contact/target in the sealed joint summary.

Snapshot locations, relocate symbols in current code: `crates/alife_world/src/headless.rs:2324–2370`; `crates/alife_world/src/headless.rs:4284–4347`; `docs/architecture/ALife_Complete_Organism_and_Intelligence_Architecture_v2.0_CONTROLLING.md:268–268`; `docs/architecture/ALife_Complete_Organism_and_Intelligence_Architecture_v2.0_CONTROLLING.md:740–746`.

Full supplied evidence remains at C:\Users\PC\Downloads\alife-recovered-evidence-7f3eea17.md under n021. Do not load the whole file.

## Steps

- [ ] Check assigned HEAD and status. Confirm this mechanism still exists and find the direct callers.
- [ ] Implement the smallest causal repair: First inspect existing channel receipts and physical outcome ABI. Preserve independent collision and consumption identities in bounded existing receipts if possible. No synthetic per-channel reward. If a schema change is unavoidable, report exact required version/codec scope to supervisor before expanding.
- [ ] Add one compact regression where practical. Acceptance scenario: Seal simultaneous blocked/collision movement plus ingestion and verify both measured physical events and their object identities survive.
- [ ] Read the complete diff and changed callers. Run only the allowed static checks. Record Rust regression as UNRUN, not passed.
- [ ] Stage exact intended files and commit with N021 in the subject. Return a concise handoff for the supervisor's manual review. Do not claim integration or runtime verification.

