# N022 — P2: Stationary and blocked actors keep their previous movement velocity in sensing implementation plan

Goal: fix N022 only. Use superpowers:executing-plans in its smallest form. The user's no-build and minimal-test constraints override conflicting skill steps.

Read README.md beside this packet for shared constraints. Architecture: existing Rust authority and contracts remain in place. This is an implementation repair, not an architecture revision.

Lane: world. Dependencies: N020. Assigned starting SHA comes from the supervisor dispatch.

## Exact initial scope

- Modify: `crates/alife_world/src/headless.rs`
- Optional one regression: `crates/alife_world/tests/headless_world_harness.rs`

Other source files may be read. If correctness needs changes outside this scope, report the smallest exact expansion to the supervisor; do not make a half-fix.

## Evidence to verify

Successful movement sets grounded_physical.velocity to displacement. Idle, Rest and blocked movement return without resetting that field. Grounded sensing later subtracts persisted observer and object velocities, so stale motion can contaminate relative-velocity features.

Snapshot locations, relocate symbols in current code: `crates/alife_world/src/headless.rs:2723–2738`; `crates/alife_world/src/headless.rs:2926–2962`; `crates/alife_world/src/grounded_sensing.rs:303–326`.

Full supplied evidence remains at C:\Users\PC\Downloads\alife-recovered-evidence-7f3eea17.md under n022. Do not load the whole file.

## Steps

- [ ] Check assigned HEAD and status. Confirm this mechanism still exists and find the direct callers.
- [ ] Implement the smallest causal repair: Set current interval velocity from committed displacement, including zero for stationary and blocked actions. Ensure later channels in the same bundle cannot erase successful locomotion velocity.
- [ ] Add one compact regression where practical. Acceptance scenario: Move then Idle/Rest or hit a blocker, and inspect both observer and neighboring-object relative velocities on the next sensory frame.
- [ ] Read the complete diff and changed callers. Run only the allowed static checks. Record Rust regression as UNRUN, not passed.
- [ ] Stage exact intended files and commit with N022 in the subject. Return a concise handoff for the supervisor's manual review. Do not claim integration or runtime verification.

