# N023 — P2: Endpoint-only collision testing allows tunneling through an obstacle implementation plan

Goal: fix N023 only. Use superpowers:executing-plans in its smallest form. The user's no-build and minimal-test constraints override conflicting skill steps.

Read README.md beside this packet for shared constraints. Architecture: existing Rust authority and contracts remain in place. This is an implementation repair, not an architecture revision.

Lane: world. Dependencies: N022. Assigned starting SHA comes from the supervisor dispatch.

## Exact initial scope

- Modify: `crates/alife_world/src/headless.rs`
- Optional one regression: `crates/alife_world/tests/headless_world_harness.rs`

Other source files may be read. If correctness needs changes outside this scope, report the smallest exact expansion to the supervisor; do not make a half-fix.

## Evidence to verify

execute_move tests blocking only at the destination. With a radius-one spherical blocker centered at zero, the unit segment (-0.5,0,0.95)→(0.5,0,0.95) has both endpoints outside the sphere but its midpoint inside it. The endpoint predicate misses that crossing.

Snapshot locations, relocate symbols in current code: `crates/alife_world/src/headless.rs:63–63`; `crates/alife_world/src/headless.rs:147–151`; `crates/alife_world/src/headless.rs:2892–2940`; `crates/alife_world/src/headless.rs:3207–3213`.

Full supplied evidence remains at C:\Users\PC\Downloads\alife-recovered-evidence-7f3eea17.md under n023. Do not load the whole file.

## Steps

- [ ] Check assigned HEAD and status. Confirm this mechanism still exists and find the direct callers.
- [ ] Implement the smallest causal repair: Use a segment closest-point swept test with existing blocker geometry. Preserve boundary tolerance and actor collision policy, and handle zero-length moves.
- [ ] Add one compact regression where practical. Acceptance scenario: Add a grazing unit-step crossing with clear endpoints, alongside ordinary endpoint-blocked and clear-motion cases.
- [ ] Read the complete diff and changed callers. Run only the allowed static checks. Record Rust regression as UNRUN, not passed.
- [ ] Stage exact intended files and commit with N023 in the subject. Return a concise handoff for the supervisor's manual review. Do not claim integration or runtime verification.

