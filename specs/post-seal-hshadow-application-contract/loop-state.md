# Post-Seal H_shadow Application Loop State

Trigger: explicit user request after full GPU runtime gap report.

Goal type: verifiable.

Goal: validated post-seal H_shadow deltas apply to live `CreatureMind` in the
static-plastic GPU smoke without changing genetic, lifetime-consolidated, or
operational layers and without requiring GPU for default paths.

Verifier: focused core/app/GPU commands plus full workspace validation.

Termination: stop when implementation validates and R2 review passes, or when a
true architecture gap prevents live application.

Budget: one implementation pass plus one review/fix pass unless validation
exposes a hard blocker.

